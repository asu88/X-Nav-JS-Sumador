


//var operandos = documents.Element('+', '#op').val()
  //= parseInt();
  
 // var operandos =  document.getElementById("op");
 // console.log(operandos);  
  
  function sumar (id) {
    var element = document.getElementById(id);
    console.log(element.innerHTML);
    //element.innerHTML = newValue;
  
  }
  
  sumar("op");
  //n_players= parseInt($('input[name=n_players]:checked', '#game_features').val()); 
  
  // http://jsfiddle.net ----para editar y ejecutar online javascript
